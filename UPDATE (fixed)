-- Script unificado com whitelist, Get Ammo, campo de texto teleport, e GUI funcional
local Players = game:GetService("Players")
local player = Players.LocalPlayer

-- ===== Configurações =====
local WHITELIST = {
"lindao_gojo3",
"tuqtc374",
"pancha_gamer1234",
"jdnekddo2",
"Dz7_Exit",
"ERrOr999898",
"NguyenphongZfypK"
}
local DISCORD_LINK = "https://discord.gg/X2k52XZwjX"

-- Teleport positions do Get Ammo
local teleportPositions = {
    Vector3.new(-45, 4, -66),
    Vector3.new(52, 4, 283),
    Vector3.new(95, 4, 179),
    Vector3.new(-7, 4, -150)
}
local REMOTE_LOOP_DURATION = 5
local TELEPORT_INTERVAL = 0.1 -- Get Ammo teleport interval
local FIRE_LOOP_STEP = 0.1

-- Teleport positions do TextBox
local textboxPositions = {
    ["1"] = Vector3.new(102, 4, -243),
    ["2"] = Vector3.new(-33, 4, 67),
    ["3"] = Vector3.new(-41, 4, 268)
}

-- ===== Funções de GUI =====
local function showCenterMessage(text,duration)
    duration = duration or 3
    local screen = Instance.new("ScreenGui")
    screen.ResetOnSpawn = false
    screen.Parent = game.CoreGui

    local frame = Instance.new("Frame", screen)
    frame.Size = UDim2.new(0.5,0,0.14,0)
    frame.Position = UDim2.new(0.5,0,0.5,0)
    frame.AnchorPoint = Vector2.new(0.5,0.5)
    frame.BackgroundColor3 = Color3.fromRGB(30,30,30)
    frame.BackgroundTransparency = 0.35
    frame.BorderSizePixel = 0
    frame.ClipsDescendants = true
    frame.AutomaticSize = Enum.AutomaticSize.None

    local label = Instance.new("TextLabel", frame)
    label.Size = UDim2.new(1,0,1,0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.new(1,1,1)
    label.Font = Enum.Font.SourceSansBold
    label.TextScaled = true

    task.delay(duration,function()
        pcall(function() screen:Destroy() end)
    end)
end

-- ===== Whitelist check =====
local authorized = false
for _,name in ipairs(WHITELIST) do
    if player.Name == name then
        authorized = true
        break
    end
end

if not authorized then
    pcall(function() setclipboard(DISCORD_LINK) end)
    showCenterMessage("Unauthorized. Discord link copied.",5)
    return
else
    showCenterMessage("Authorization confirmed :3 Thanks for using my script",5)
end

-- ===== Espera personagem pronto =====
local function waitChar()
    local ch = player.Character or player.CharacterAdded:Wait()
    local root = ch:WaitForChild("HumanoidRootPart",5)
    return ch, root
end

-- ===== Criação do GUI =====
local gui = Instance.new("ScreenGui")
gui.Name = "GetAmoGUI"
gui.Parent = game.CoreGui

-- ===== Botão Get Ammo =====
local button = Instance.new("TextButton")
button.Size = UDim2.new(0,120,0,40)
button.Position = UDim2.new(1,-130,0,10)
button.AnchorPoint = Vector2.new(0,0)
button.BackgroundColor3 = Color3.fromRGB(60,180,75)
button.TextColor3 = Color3.new(1,1,1)
button.Font = Enum.Font.SourceSansBold
button.TextSize = 20
button.Text = "Get Ammo"
button.Active = true
button.Draggable = true
button.Parent = gui

-- ===== Campo de Texto =====
local textBox = Instance.new("TextBox")
textBox.Size = UDim2.new(0,120,0,40)
textBox.Position = UDim2.new(1,-130,0,60)
textBox.AnchorPoint = Vector2.new(0,0)
textBox.BackgroundColor3 = Color3.fromRGB(50,50,50)
textBox.BackgroundTransparency = 0.5
textBox.TextColor3 = Color3.new(1,1,1)
textBox.Font = Enum.Font.SourceSansBold
textBox.TextSize = 18
textBox.PlaceholderText = "Digite 1,2 ou 3"
textBox.ClearTextOnFocus = false
textBox.Active = true
textBox.Draggable = true
textBox.Parent = gui
textBox.TextStrokeTransparency = 0.8
textBox.TextWrapped = true
textBox.TextScaled = true
textBox.ClipsDescendants = true

-- ===== Funções Fire ClickDetector =====
local availableFire = nil
local function detectFireFunctions()
    local candidates = {
        "fireclickdetector",
        "clickdetectorfire",
        "fireClickDetector",
        "FireClickDetector",
        "fireclickdetector_internal",
        "fireclickdetectorremote",
        "FireClickDetectorInternal"
    }
    for _, name in ipairs(candidates) do
        local f = rawget(_G,name) or (type(_G[name])=="function" and _G[name]) or _G[name]
        if type(f)=="function" then
            return f,name
        end
    end
    if type(fireclickdetector)=="function" then return fireclickdetector,"fireclickdetector" end
    if type(clickdetectorfire)=="function" then return clickdetectorfire,"clickdetectorfire" end
    return nil,nil
end
availableFire,_ = detectFireFunctions()

local function tryFireDetector(detector)
    if not detector or not detector:IsA("ClickDetector") then return false,"invalid" end
    if availableFire then
        local ok,err = pcall(function() availableFire(detector) end)
        if ok then return true,"exploit_func" end
    end
    local success,gc = pcall(function() return getconnections end)
    if success and typeof(gc)=="function" then
        local ok,conns = pcall(function() return getconnections(detector.MouseClick) end)
        if ok and type(conns)=="table" and #conns>0 then
            for _,c in ipairs(conns) do
                pcall(function()
                    if type(c.Fire)=="function" then pcall(function() c:Fire(player) end)
                    elseif type(c.Function)=="function" then pcall(function() c.Function(player) end)
                    elseif type(c)=="function" then pcall(function() c(player) end)
                    elseif type(c)=="table" and type(c.Function)=="function" then pcall(function() c.Function(player) end)
                    end
                end)
            end
            return true,"getconnections"
        end
    end
    return false,"failed"
end

-- ===== Função coletar ClickDetectors de Ammo =====
local function collectDetectors()
    local a = workspace:FindFirstChild("AmmoPiles")
    if not a then return {} end
    local detectors = {}
    local children = a:GetChildren()
    local targets = {children[4],children[3],a:FindFirstChild("LeftAmmoPile"),a:FindFirstChild("RightAmmoPile")}
    for _,item in ipairs(targets) do
        if item and item:FindFirstChild("Detector") then
            local cd = item.Detector:FindFirstChildOfClass("ClickDetector")
            if cd then table.insert(detectors,cd) end
        end
    end
    return detectors
end

local function fireAllFor(detectors,duration)
    if #detectors==0 then return end
    local t0 = tick()
    while tick()-t0 < duration do
        for _,d in ipairs(detectors) do
            pcall(function() tryFireDetector(d) end)
        end
        task.wait(FIRE_LOOP_STEP)
    end
end

-- ===== Função Get Ammo =====
button.MouseButton1Click:Connect(function()
    button.Active = false
    button.Draggable = false

    local ch,root = waitChar()
    local original = root.CFrame
    local detectors = collectDetectors()

    if #detectors==0 then
        button.Active = true
        button.Draggable = true
        return
    end

    for _,pos in ipairs(teleportPositions) do
        pcall(function() root.CFrame = CFrame.new(pos + Vector3.new(0,3,0)) end)
        task.spawn(function() fireAllFor(detectors,REMOTE_LOOP_DURATION) end)
        task.wait(TELEPORT_INTERVAL)
    end

    task.wait(0.2)
    pcall(function() root.CFrame = original end)

    button.Active = true
    button.Draggable = true
end)

-- ===== Função TextBox teleport =====
textBox.FocusLost:Connect(function(enterPressed)
    local value = textBox.Text
    local targetPos = textboxPositions[value]
    if targetPos then
        local ch,root = waitChar()
        root.CFrame = CFrame.new(targetPos + Vector3.new(0,3,0))
    end
end)
